```typescript
export const EmailCard = () => {
  const { email } = useUserProvider();
  // Other codes
};

export const BadgeCard = () => {
  const { badge } = useUserProvider();
  // Other codes
};
```
